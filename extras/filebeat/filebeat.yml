###################### Filebeat Configuration #######################

http.enabled: true
http.port: 5066
http.host: "${ES_NET}.9"

monitoring:
  enabled: false
  cluster_uuid: "${CLUSTER_UUID}"

filebeat.config:
  modules:
    path: ${path.config}/modules.d/*.yml
    reload.enabled: false

output:
  # for provisioning:
  logstash:
    enabled: true
    hosts: ["logstash.${APP_NET}:5045"]
    # ssl:
      ## certificate_authorities: ["${ELASTICSEARCH_SSL_CERTIFICATEAUTHORITIES}"]
      # certificate: "${LOGSTASH_SERVER_CERT}"
      # key: "${LOGSTASH_SERVER_CERT_KEY}"
      # verification_mode: "${VERIFICATION_MODE}"
      # verification_mode: "none"
   
  # for /filebeat -e setup --dashboards:
  # elasticsearch:
  #   hosts: ["https://es01.${APP_NET}:9200"]
  #   protocol: "https"
  #   username: "${ELASTICSEARCH_USERNAME}"
  #   password: "${ELASTICSEARCH_PASSWORD}"
  #   ssl:
  #     enabled: true
  #     certificate_authorities: ["${ELASTICSEARCH_SSL_CERTIFICATEAUTHORITIES}"]
  #     verification_mode: "${VERIFICATION_MODE}"

setup:      
  ilm.overwrite: true
  template.settings:
    index.number_of_shards: 2
    #index.codec: best_compression
    #_source.enabled: false
  elasticsearch:
    hosts: ["https://es01.${APP_NET}:9200"]
    protocol: "https"
    username: "${ELASTICSEARCH_USERNAME}"
    password: "${ELASTICSEARCH_PASSWORD}"
    ssl:
      enabled: true
      certificate_authorities: ["${ELASTICSEARCH_SSL_CERTIFICATEAUTHORITIES}"]
      verification_mode: "${VERIFICATION_MODE}"
  
  kibana:
    host: "http://kibana.${APP_NET}:8080"
    # host: "https://kibana.${APP_NET}:5601"
    username: '${ELASTICSEARCH_USERNAME}'
    password: '${ELASTICSEARCH_PASSWORD}'
    protocol: "http"
    # ssl:
    #  enabled: true
    #  certificate_authorities: ['${ELASTICSEARCH_SSL_CERTIFICATEAUTHORITIES}']
    #  erification_mode: "${VERIFICATION_MODE}"
