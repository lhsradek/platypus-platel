
#
# === Filebeat ===
#
  filebeat:
    depends_on:
      logstash:
        condition: service_healthy
    # Ubuntu - filebeat
    image: docker.elastic.co/beats/filebeat:${STACK_VERSION}
    restart: ${DOCKER_RESTART_POLICY}
    container_name: "${APP_ID}-filebeat"
    hostname: "filebeat"
    networks:
      # for traefik:
      #  - default
      # local network:
      - intnet
    user: root
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      # - /sys/fs/cgroup:/hostfs/sys/fs/cgroup:ro
      # - /proc:/hostfs/proc:ro
      - /:/hostfs:ro
      - ./certs:/usr/share/filebeat/certs
      - ./extras/filebeat/filebeat.yml:/usr/share/filebeat/filebeat.yml:ro
      - ./logs/filebeat:/usr/share/filebeat/logs
      - ./logs/apm:/usr/share/filebeat/elogs/apm-server
      - ./logs/es01:/usr/share/filebeat/elogs/es01
      - ./logs/es02:/usr/share/filebeat/elogs/es02
      - ./logs/kibana:/usr/share/filebeat/elogs/kibana
      - /root/platypus-lhsdock/logs/nginx:/usr/share/filebeat/elogs/nginx/default
      - /root/platypus-wordpress-mysql/logs/wordpress-nginx/:/usr/share/filebeat/elogs/nginx/platypus-wordpress-mysql
      - /root/platypus-wordpress-mysql/logs/wordpress-mysql/:/usr/share/filebeat/elogs/mysql/platypus-wordpress-mysql
      - /root/platypus-box/logs/traefik:/usr/share/filebeat/elogs/traefik
      - filebeat:/usr/share/filebeat
    environment:
      - CLUSTER_NAME=${CLUSTER_NAME}
      - CLUSTER_UUID=${CLUSTER_UUID}
      - ELASTICSEARCH_USERNAME=elastic
      - ELASTICSEARCH_PASSWORD=${KIBANA_PASSWORD}
      - ELASTICSEARCH_SSL_CERTIFICATEAUTHORITIES=/usr/share/filebeat/certs/ca/ca.crt
      - LOGSTASH_SERVER_CERT=/usr/share/filebeat/certs/logstash/logstash.crt
      - LOGSTASH_SERVER_CERT_KEY=/usr/share/filebeat/certs/logstash/logstash.key
      - VERIFICATION_MODE=${VERIFICATION_MODE}        
#     labels:
#       - "traefik.enable=true"
#       - "traefik.docker.network=${DOCKER_NETWORK}"
#       - "traefik.http.routers.filebeat-${APP_ID}.rule=Host(`filebeat`)"
#       - "traefik.http.routers.filebeat-${APP_ID}.entrypoints=https"
#       - "traefik.http.routers.filebeat-${APP_ID}.tls=true"
#       - "traefik.http.services.filebeat-${APP_ID}.loadbalancer.server.port=5066"
#       - "traefik.http.services.filebeat-${APP_ID}.loadbalancer.server.scheme=http"
